# Path to your Foundry VTT install root (the directory containing Data/, App/, Config/...).
# Accepts a Windows path (auto-converted via wslpath under WSL) or a native Unix path.
FOUNDRY_PATH=C:\Users\jared\FoundryVTT-WindowsPortable-14.359
